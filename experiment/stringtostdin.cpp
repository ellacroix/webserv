#include <iostream>
#include <unistd.h>
#include <stdio.h>
#include <string>
#include <cstdio>
#include <sstream>
#include <stdlib.h>
#include <fcntl.h>

int main() {
      std::string string("Test");
      int fd;

      read(fd, )


      dup2(STDIN_FILENO, psbuf);

      execlp("/usr/bin/cat", "cat", NULL);
}